apiVersion: apps/v1
kind: Deployment
metadata:
  name: photo-sharing-api
spec:
  template:
    spec:
      containers:
        - name: photo-sharing-api
          env:
            - name: MONGODB_ADDRESS
              value: mongodb-internal.bcb254-dev.svc